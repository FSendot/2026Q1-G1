# AWS Academy lab: intentional trade-offs documented in ARCHITECTURE.md and inline
# checkov:skip comments. Central list keeps pre-commit from blocking on checks that
# cannot be satisfied in the sandbox (no KMS CMK, no IAM role creation, public S3 dashboard).
skip-check:
  - CKV_AWS_115   # Lambda reserved concurrency — not configured in lab
  - CKV_AWS_173   # Lambda env var KMS encryption — no CMK in Academy
  - CKV_AWS_309   # CORS OPTIONS routes intentionally unauthenticated
  - CKV_AWS_51    # ECR mutable tags — deploy workflow uses commit SHA tags
  - CKV_AWS_54    # Dashboard S3 static website requires public access
  - CKV_AWS_55
  - CKV_AWS_53
  - CKV_AWS_56
  - CKV_AWS_70    # Dashboard bucket policy allows public read
  - CKV_AWS_300   # Audit bucket lifecycle — abort incomplete multipart out of lab scope
  - CKV_AWS_226   # RDS auto minor version upgrade — lab footprint
  - CKV_AWS_161   # RDS IAM auth — proxy uses Secrets Manager credentials
  - CKV_AWS_353   # RDS Performance Insights — lab cost
  - CKV2_AWS_60   # RDS copy tags to snapshots — lab
  - CKV_AWS_145   # S3 KMS default encryption — no CMK in Academy
  - CKV2_AWS_6    # Dashboard bucket public access block defaults
  - CKV_AWS_21    # Dashboard S3 versioning — static files regenerated by Terraform
  - CKV2_AWS_30   # RDS PostgreSQL query logging — lab
  - CKV2_AWS_5    # RDS Proxy SG attached via ENI at apply time; Checkov false positive
